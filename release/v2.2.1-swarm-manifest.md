# v2.2.1 Swarm Manifest

Date: 2026-05-15
Release branch: `release/v2.2.1`
Release worktree: `/tmp/thufir-worktrees/release/release-v2.2.1`

## Branch parity

- `origin/main...origin/develop`: `205 7`
- Interpretation: historical protected-branch drift remains unresolved.
- Sprint rule: all `v2.2.1` work starts from `origin/develop` and promotes through `develop -> main` after release validation.

## Release focus

`v2.2.1` is a wiring-correctness sprint for the adaptive learning loop.

Primary production issues to fix:
- dedicated retrieval/policy-trace columns remain null
- full close can downgrade `v2.2` dossiers back to `v2.1`
- explicit counterfactual and similarity-feature tables are unwired
- adaptive policy persistence/consumption is unwired

## Task Map

### 1. Autonomous dossier wiring

- Branch: `feat/v2.2.1-autonomous-dossier-wiring`
- Worktree: `/tmp/thufir-worktrees/agent-autonomous/feat-v2.2.1-autonomous-dossier-wiring`
- Scope:
  - `src/core/autonomous.ts`
  - `tests/core/autonomous-wiring.test.ts`
  - `tests/memory/trade-dossiers.test.ts`
- Acceptance:
  - all autonomous `v2.2` dossier writes persist top-level `retrieval` and `policyTrace`
  - blocked and executed-open paths are covered

### 2. Close-path adaptive persistence

- Branch: `feat/v2.2.1-close-path-adaptive-persistence`
- Worktree: `/tmp/thufir-worktrees/agent-close/feat-v2.2.1-close-path-adaptive-persistence`
- Scope:
  - `src/core/tool-executor.ts`
  - `src/core/perp_lifecycle.ts`
  - `src/core/trade_similarity_feature_derivation.ts`
  - `tests/core/tool-executor-perps.test.ts`
  - `tests/core/trade-similarity-feature-derivation.test.ts`
- Acceptance:
  - full close preserves `v2.2`
  - counterfactual rows persist
  - similarity feature rows persist

### 3. Adaptive policy runtime

- Branch: `feat/v2.2.1-adaptive-policy-runtime`
- Worktree: `/tmp/thufir-worktrees/agent-policy/feat-v2.2.1-adaptive-policy-runtime`
- Scope:
  - `src/core/adaptive_decision_enforcer.ts`
  - `src/core/trade_similarity.ts`
  - `src/core/trade_policy_adaptation.ts`
  - `src/memory/trade_policy_adjustments.ts`
  - targeted runtime/policy tests
- Acceptance:
  - active policy adjustments are persisted and consumed
  - policy traces include active adjustment ids
  - bounded enforcement tests pass

## Merge Order

1. `feat/v2.2.1-autonomous-dossier-wiring`
2. `feat/v2.2.1-close-path-adaptive-persistence`
3. `feat/v2.2.1-adaptive-policy-runtime`

## PR Target Rule

- All feature branches must target `release/v2.2.1`.
- No feature PR may target `develop` or `main`.

## Release Gate

Before promoting `release/v2.2.1`:
- targeted branch tests must pass for every task branch
- release branch must pass focused adaptive suite
- release branch must prove paper open/close and blocked-path persistence behavior
