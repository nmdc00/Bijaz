# v1.91 Sprint Progress

## Status: COMPLETE (6 of 6 branches merged)
## Date: 2026-03-07

## Completed Branches (this session)

### 1. `feat/v1.91-tool-subset-registry` — MERGED (PR #152)
- Defined tool subset categories: discovery (13), execution (12), chat (33), trivial (4), full (50)
- Added `getToolsForSubset()` and `getToolSubsetNames()` exports
- Wired `AgenticAnthropicClient` and `AgenticOpenAiClient` with `toolSubset` param
- Updated `createAgenticExecutorClient` to pass through `toolSubset`
- 10 tests passing
- Files: `src/core/tool-schemas.ts`, `src/core/llm.ts`, `tests/core/tool-subset-registry.test.ts`

### 2. `feat/v1.91-signal-cache-layer` — MERGED (PR #153)
- Added `TTLCache<V>` utility class in `src/discovery/signal_cache.ts`
- Wrapped all 5 signal functions with TTL caching (default 5 min)
- Cache keyed by signal type + symbol
- Configurable via `discovery.signalCacheTtlSeconds`
- 10 tests passing
- Files: `src/discovery/signal_cache.ts`, `src/discovery/signals.ts`, `tests/discovery/signal-cache.test.ts`

### 3. `feat/v1.91-identity-strip-trivial` — MERGED (PR #154)
- Changed default `internalPromptMode` from `'minimal'` to `'none'` for trivial calls
- Added early return in `finalizeMessages` when mode is `'none'`: skips identity loading entirely
- Exported `resolveIdentityPromptMode` and `finalizeMessages` for testability
- 8 tests passing
- Files: `src/core/llm.ts`, `tests/core/identity-strip-trivial.test.ts`

## Full Regression: 119 test files, 438 tests, all passing on release/v1.91

### 4. `feat/v1.91-autonomous-scan-bypass` — MERGED (PR #155)
- `AutonomousManager` now receives `infoLlm ?? llm` (trivial/direct) instead of OrchestratorClient
- Scan pipeline (discovery → filter → evaluate) confirmed 0 LLM calls
- Async enrichment synthesis step: 1 direct call instead of 2 (plan + execute via orchestrator)
- Wired `toolSubset: 'execution'` to `createAgenticExecutorClient` in conversation.ts
- Removed dead `autonomyLlm` field and its executor/config setup from agent.ts
- 3 tests passing
- Files: `src/core/agent.ts`, `src/core/conversation.ts`, `tests/core/autonomous-scan-bypass.test.ts`

## Full Regression: 120 test files, 441 tests, all passing on feat/v1.91-autonomous-scan-bypass

### 5. `feat/v1.91-context-budget-scoping` — MERGED (PR #156)
- Added `agent.promptBudget` config schema: `{autonomous: 25K, trivial: 10K, chat: 120K}`
- Exported `resolveMaxPromptChars(config, meta)` — checks `meta.kind` and `getExecutionContext().source`
- Wired into both `trimMessagesByCharBudget` call sites in `finalizeMessages`
- `trivial` kind always wins over execution context (local model cannot handle big contexts)
- 10 tests passing
- Files: `src/core/config.ts`, `src/core/llm.ts`, `config/default.yaml`, `tests/core/context-budget-scoping.test.ts`

## Full Regression: 121 test files, 451 tests, all passing on feat/v1.91-context-budget-scoping

### 6. `feat/v1.91-efficiency-integration-acceptance` — MERGED (PR #157)
- 6 assertions: tool schema size (≥60% smaller for discovery/execution vs full), identity strip, context budget ceilings, overall ≥40% token reduction, 0 LLM calls in scan pipeline
- Baseline: 2 orchestrator calls (plan + execute) × full schema + identity = high token count
- After: 1 trivial call × no schema + no identity + 10K cap = ≥40% reduction confirmed
- Files: `tests/core/efficiency-integration-acceptance.test.ts`

## Full Regression: 124 test files, 481 tests, all passing on feat/v1.91-efficiency-integration-acceptance

## Spec Coverage Additions (post-merge)

### Untracked test files (to commit)
- `tests/core/executor-toolsubset-wiring.test.ts` — 2 tests: ConversationHandler passes `toolSubset='execution'` to `createAgenticExecutorClient`
- `tests/discovery/signal-cache-integration.test.ts` — 20 tests: end-to-end dedup for all 5 signal functions + custom TTL expiry via fake timers

### Gaps filled in existing files
- `tests/core/config.validation.test.ts` — added 4 assertions: `internalPromptMode='minimal'`, `promptBudget.{autonomous,trivial,chat}` defaults
- `tests/core/context-budget-scoping.test.ts` — added 2 tests: fallback to `agent.maxPromptChars` when `promptBudget` absent; fallback to 120000 hard-coded default
- `tests/discovery/signal-cache-integration.test.ts` — added TTL expiry test: custom `signalCacheTtlSeconds=1` expires mid-window via `vi.useFakeTimers()`

## Wiring TODO (next session)
- ~~Callers need to pass `toolSubset` when creating agentic clients~~ DONE: `'execution'` wired in conversation.ts executor model path (branch #4)
